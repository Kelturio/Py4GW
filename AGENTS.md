# Repository Instructions

- Before working in this repository, always ensure the Git submodules are initialized and up to date by running `git submodule update --init --recursive` or the helper script in `scripts/init_submodules.py`.
- Follow general project conventions described in `readme.txt` and other documentation when modifying files.
